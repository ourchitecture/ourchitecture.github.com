################################################################################
# Runs checks and install on all PRs as well as branch pushes except "main".
################################################################################
name: continuous_integration

# place "magic strings" used in the workflow here for convenient maintenance
env:
  CI: true

on:
  push:
    branches: ["*", "!main"]
  pull_request:
    branches: ["*"]

jobs:
  integration:
    # Cannot use `env`: https://github.com/actions/runner/issues/1189#issuecomment-880110759
    # do not run on forks
    if: ${{ github.repository == 'ourchitecture/ourchitecture.github.com' }}

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          # See: https://github.com/actions/setup-node/blob/main/docs/advanced-usage.md#caching-packages-data
          cache: "yarn"
          cache-dependency-path: "./**/yarn.lock"

      - name: install
        run: |
          make install
